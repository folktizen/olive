{
  "name": "@useolive/monorepo",
  "repository": "https://github.com/moojok/olive",
  "version": "1.4.0",
  "license": "MIT",
  "private": true,
  "packageManager": "bun@1.2.13",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/sdk && bun typechain && cd ../.. && turbo run build lint",
    "build:app": "cd packages/sdk && bun typechain && turbo run build --filter=@useolive/sdk --filter=@useolive/app",
    "build:landing": "cd packages/sdk && bun typechain && turbo run build --filter=@useolive/sdk --filter=@useolive/landing",
    "build:contracts": "turbo run build --filter=@useolive/contracts",
    "dev": "turbo run dev",
    "dev:app": "cd packages/app && turbo run dev",
    "dev:landing": "cd packages/landing && turbo run dev",
    "setup": "bun install && bun run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "export": "turbo run export",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@svgr/webpack": "^8.0.1",
    "@types/umami": "^2.10.0",
    "eslint": "^8.28.0",
    "eslint-config-turbo": "^2.5.3",
    "turbo": "^2.0.14"
  },
  "trustedDependencies": [
    "@trufflesuite/bigint-buffer",
    "bufferutil",
    "es5-ext",
    "keccak",
    "leveldown",
    "protobufjs",
    "secp256k1",
    "sharp",
    "unrs-resolver",
    "utf-8-validate"
  ]
}