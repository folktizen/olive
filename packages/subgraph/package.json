{
  "name": "@useolive/subgraph",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "auth": "graph auth $DEPLOY_KEY",
    "codegen": "graph codegen",
    "build:ethereum": "bun run prepare:ethereum && graph build",
    "build:polygon": "bun run prepare:polygon && graph build",
    "build:arbitrum-one": "bun run prepare:arbitrum-one && graph build",
    "build:base": "bun run prepare:base && graph build",
    "build:gnosis": "bun run prepare:gnosis && graph build",
    "prepare:ethereum": "ts-node bin/build-subgraph.ts mainnet && bun run codegen",
    "prepare:polygon": "ts-node bin/build-subgraph.ts matic && bun run codegen",
    "prepare:arbitrum-one": "ts-node bin/build-subgraph.ts arbitrum-one && bun run codegen",
    "prepare:base": "ts-node bin/build-subgraph.ts base && bun run codegen",
    "prepare:gnosis": "ts-node bin/build-subgraph.ts xdai && bun run codegen",
    "deploy:ethereum": "bun run prepare:ethereum && graph deploy olv-on-ethereum",
    "deploy:polygon": "bun run prepare:polygon && graph deploy olv-on-polygon",
    "deploy:arbitrum-one": "bun run prepare:arbitrum-one && graph deploy olv-on-arbitrum-one",
    "deploy:base": "bun run prepare:base && graph deploy olv-on-base",
    "deploy:gnosis": "bun run prepare:gnosis && graph deploy olv-on-gnosis",
    "create-local": "graph create --node http://localhost:8020/ folktizen/olive",
    "remove-local": "graph remove --node http://localhost:8020/ folktizen/olive",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 folktizen/olive",
    "test": "echo 'No tests yet, implement them'",
    "lint": "bunx prettier --check . --ignore-path ../../.prettierignore",
    "format": "bunx prettier --write . --ignore-path ../../.prettierignore"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.78.0",
    "@graphprotocol/graph-ts": "0.35.1",
    "@types/node": "^18.11.18",
    "ts-node": "^10.8.2",
    "yaml": "^2.2.1"
  }
}
